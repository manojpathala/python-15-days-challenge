#modules are in-built functions which is used to do complex problems into simple
#using import we can call the modules which we want
#if we want any module not installed then usin pip install and after pip install type the required module then it is installed 

from calc import *
a=7
b=6
c=sub(a,b)
print(c)


